# git-https-07 — implementation record

## Status

- Branch: `feat/git-https-pat`
- Completion: **done**

## Summary

Repository overview shows HTTPS clone URL from `useGitConfig` with www-stripped fallback. PAT settings linked from clone card. SSH clone URL and nav hidden when `sshEnabled` is false. Settings and ssh-keys pages show disabled note.

## Tests

- Manual UI verification
