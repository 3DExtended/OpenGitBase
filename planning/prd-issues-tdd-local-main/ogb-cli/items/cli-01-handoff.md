# Handoff — cli-01

## Work item

CLI project bootstrap — `applications/OpenGitBase.Cli/`, System.CommandLine root, `--help`, `--version`, solution wiring.

## Branch

`main`

## Acceptance criteria

- New console project builds in solution
- Executable name `ogb`
- `ogb --help` and `ogb --version` work
- CI/build includes CLI project

## Dependencies

None
