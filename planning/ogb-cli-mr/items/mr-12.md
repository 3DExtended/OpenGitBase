# Compose E2E smoke

## Metadata

- ID: mr-12
- Type: AFK
- Status: ready
- Source: docs/prd/ogb-cli-mr.md

## Parent

[PRD: `ogb mr` — Merge Request CLI Commands](../../../docs/prd/ogb-cli-mr.md)

## What to build

Add compose-backed E2E: **`CliMrE2eTests`** (or extend merge-request tier) exercising **`ogb mr create`**, **`list`**, **`view`**, and **`close`** against `localhost:8089/api`. Optional shell script **`scripts/test-ogb-cli-mr-e2e.sh`** mirroring `scripts/test-ogb-cli-e2e.sh`.

## Acceptance criteria

- [ ] E2E test tagged `RequiresCompose` and Category `Cli` or `MergeRequest`
- [ ] Passes against healthy compose stack (storage + dispatchers)
- [ ] Uses registered user with verified email and pushed branch (or MergeRequestFixture pattern)
- [ ] Smoke script documented in planning index verification section

## Blocked by

- [mr-11](./mr-11.md)

## User stories covered

- 45, 46, 47 — agent loop against real stack

## Notes

- Requires `docker-compose.e2e.yml` overlay if tests depend on email capture (not needed for basic MR smoke).
- Merge/approve E2E optional if compose seed is heavy; create/list/close minimum.
